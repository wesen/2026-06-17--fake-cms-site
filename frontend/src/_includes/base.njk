<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title or cms.site.name }}</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <header class="site-header"><a href="/">{{ cms.site.name }}</a></header>
    <main class="site-main">{{ content | safe }}</main>
  </body>
</html>
